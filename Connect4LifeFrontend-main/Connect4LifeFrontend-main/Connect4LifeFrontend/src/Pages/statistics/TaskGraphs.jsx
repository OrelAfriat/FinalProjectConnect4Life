// src/Pages/statistics/TaskGraphs.jsx
import React from 'react'
import TaskGraphContainer from './TaskGraphContainer'

const TaskGraphs = ({ tasks }) => {
  return <TaskGraphContainer tasks={tasks} />
}

export default TaskGraphs
